{
  "name": "mars",
  "version": "1.0.0",
  "description": "A copilot for competive exams",
  "scripts": {
    "build": "rm -r dist & tsc",
    "start": "npm run build && node dist/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Genesis",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.7",
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "typeorm": "^0.3.17",
    "typescript": "^5.2.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.17"
  }
}
